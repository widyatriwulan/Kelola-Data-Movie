1. Unit Testing

    DetailMovieViewModelTest :
                           - memanipulasi data ketika pemanggilan data movies di kelas repository
                           - memastikan metode di kelas repository terpanggil
                           - melakukan pengecekan data movies apakah null/tidak
                           - membandingkan data movies sudah sesuai dengan yang diharapkan/tidak
                           - memastikan ada perubahan data didalam LiveData sesuai yang diharapkan
    DetailTvShowViewModelTest :
                           - memanipulasi data ketika pemanggilan data tvshow di kelas repository
                           - memastikan metode di kelas repository terpanggil
                           - melakukan pengecekan data tvshow apakah null/tidak
                           - membandingkan data tvshow sudah sesuai dengan yang diharapkan/tidak
                           - memastikan ada perubahan data didalam LiveData sesuai yang diharapkan
    MoviesViewModelTest :
                          - memanipulasi data ketika pemanggilan data movies di kelas repository
                          - memastikan metode di kelas repository terpanggil
                          - melakukan pengecekan data movies apakah null/tidak
                          - melakukan pengecekan jumlah data movies apakah sudah sesuai/belum
                          - memastikan ada perubahan data didalam LiveData sesuai yang diharapkan

    TvShowViewModelTest :
                          - memanipulasi data ketika pemanggilan data tvshow di kelas repository
                          - memastikan metode di kelas repository terpanggil
                          - melakukan pengecekan data tvshow apakah null/tidak
                          - melakukan pengecekan jumlah data tvshow apakah sudah sesuai/belum
                          - memastikan ada perubahan data didalam LiveData sesuai yang diharapkan

    FilmRepositoryTest :
                          - memastikan data movies yang ada di dalam LiveData sesuai yg diharapkan
                          - melakukan pengecekan data movies apakah null/tidak
                          - melakukan pengecekan data movies(title) apakah null/tidak
                          - melakukan pengecekan data movies(rilis) apakah null/tidak
                          - melakukan pengecekan data movies(genre) apakah null/tidak
                          - melakukan pengecekan data movies(description) apakah null/tidak
                          - melakukan pengecekan data movies(image) apakah null/tidak
                          - membandingan data movies sudah sesuai dengan yang diharapkan/tidak

                          - memastikan data tvshow yang ada di dalam LiveData sesuai yg diharapkan
                          - melakukan pengecekan data tvshow apakah null/tidak
                          - melakukan pengecekan data tvshow(title) apakah null/tidak
                          - melakukan pengecekan data tvshow(tahun) apakah null/tidak
                          - melakukan pengecekan data tvshow(genre) apakah null/tidak
                          - melakukan pengecekan data tvshow(description) apakah null/tidak
                          - melakukan pengecekan data tvshow(image) apakah null/tidak
                          - membandingan data tvshow sudah sesuai dengan yang diharapkan/tidak

2. Instrumental Testing (DaftarActivityTest)
        -loadMovie(Menampilkan data movie)
             - memastikan rv_movies dalam keadaan tampil
             - gulir rv_movies ke posisi data terakhir
        -loadDetailMovie (Menampilkan data detail movie)
            - menampilkan tindakan klik pada data pertama di rv_movies
            - memastikan textView untuk title tampil sesuai dengan yang diharapkan
            - memastikan textView untuk description tampil sesuai dengan yang diharapkan
            - memastikan textView untuk rilis tampil sesuai dengan yang diharapkan
            - memastikan textView untuk genre tampil sesuai dengan yang diharapkan
            - memastikan textView untuk director tampil sesuai dengan yang diharapkan
            - memastikan textView untuk screenplay tampil sesuai dengan yang diharapkan
        - loadtvshow(Menampilkan data tvshow)
            - menampilkan tindakan klik tvshow
            - memastikan rv_tvshow dalam keadaan tampil
            - gulir rv_tvshow ke posisi data terakhir
        - loadDetailTvshow (Menampilkan data detail tvshow)
            - menampilkan tindakan klik tvshow
            - memastikan rv_tvshow dalam keadaan tampil
            - menampilkan tindakan klik pada data pertama di rv_tvshow
            - memastikan textView untuk title tampil sesuai dengan yang diharapkan
            - memastikan textView untuk description tampil sesuai dengan yang diharapkan
            - memastikan textView untuk tahun tampil sesuai dengan yang diharapkan
            - memastikan textView untuk genre tampil sesuai dengan yang diharapkan